<img src="docs/app-icon.png" width="96" alt="FamilyShare">

# 家庭共享（FamilyShare）

> [English](README.en.md) | **中文**

一个**家庭私享的位置共享 App**：家人用 6 位家庭码申请加入同一家庭（**需群主同意**），在地图上实时看到彼此位置；支持低功耗后台上报、开机自启、应用内更新、响铃、轨迹、黑名单、消息中心等。坐标使用高德坐标系（GCJ-02）。

- 📱 客户端：`android/`（原生 Android，Java，高德地图 SDK 3dmap 9.8.3 + OkHttp）
- ☕ 服务端：`server/`（Java / Spring Boot 3.3.4，REST + WebSocket，Redis 持久化）
- 📄 文档：`docs/`（协议、高德 Key 申请、图标）
- 🌐 官网：[https://fms.uiero.com](https://fms.uiero.com)

> 本项目为“家庭自用”而设计：**无账号体系**（`deviceId` 即身份、家庭码即授权凭证），位置数据仅存储到你自己部署的服务器。生产环境建议加 HTTPS/WSS 反代，并妥善设置访问口令。

---

## ✨ 功能特性

| 功能 | 说明 |
| --- | --- |
| 实时位置共享 | 6 位家庭码申请加入（需群主同意）→ 地图彩色图钉实时展示；上下线状态实时变化；成员列表头像与地图标点同色 |
| 低功耗后台 + 开机自启 | 前台服务（`dataSync`）+ `AlarmManager.setAndAllowWhileIdle` 自调度 + `BOOT_COMPLETED` 自动拉起 + 5 分钟看门狗 |
| 高德地图 | 完整版 3D 地图 SDK 9.8.3（原生渲染，底图与标注同层，**缩放时地名不会漂移**），GCJ-02 坐标，自绘刻度尺 |
| 实时刷新 | 家人点「刷新」→ 服务端 WebSocket 下发 `report-now` → 目标立即定位上报 → 全家人秒级更新 |
| 打开自动刷新 | 每次打开 App（含从后台返回）且在多人家庭时，自动向全员请求一次实时位置并底部提示 |
| 成员详情 | 电量、网络（含 WiFi 名称）、粗略地址、精度、一键导航、响铃 |
| 响铃 | 详情页「响铃」→ 目标手机响铃 30 秒 + 通知 |
| 轨迹 | 任意成员可为成员/自己开启轨迹（1/3/5 分钟或自定义间隔）→ 地图同色连线 + 方向箭头 |
| 下线模式 | ⋮ 菜单「下线/上线」：开启后自己不更新位置、对他人显示灰色离线 |
| 黑名单 | 创建者移出时可「仅移出 / 移出并拉黑」；⋮「黑名单管理」查看/解除 |
| 消息中心 | 集中展示入群申请（群主可同意/拒绝）与加入邀请 |
| 应用内更新 | 启动静默检查 + ⋮「检查更新」；MD5 比对；下载进度 + 校验 + 一键安装 |
| 反馈/Bug 上报 | ⋮「反馈Bug」→ 服务端写入 `server/bugs.json`，附管理页 `/bugadmin/<token>` |
| 服务器切换 | ⋮ 菜单「切换服务器」：可选择官方（不展示地址）或自定义服务器（展示地址）；可添加（域名/IP + 可选端口 + http/https）与删除；默认官方，切换后自动重连 |
| 网络重试 | 对瞬时网络/DNS 错误自动有限重试，并给出更友好的提示（如“域名解析失败，请检查网络或 DNS”） |

---

## 🧱 技术栈

| 层 | 技术 |
| --- | --- |
| 客户端 | Java、Gradle / AGP 8.5.2 / Gradle 8.7 / JDK 17+，minSdk 23 / targetSdk 34 |
| 地图/定位 | 高德**完整版 3D 地图 SDK 9.8.3**（Maven：`com.amap.api:3dmap:9.8.3`，已内置定位，勿再单独引入 `com.amap.api:location`），原生渲染、自带 `.so`，release 包约 17MB |
| 网络 | OkHttp 4.12.0（REST + WebSocket） |
| UI | Material 1.12.0 / AppCompat / RecyclerView |
| 服务端 | Spring Boot 3.3.4（starter-web + starter-websocket），Java 17，Maven，内嵌 Tomcat，端口 3000 |
| 持久化 | Redis（按实体分 key，每 2 秒全量同步 + 删除对账）；头像为文件、Bug 反馈为文件 |

---

## 📥 环境要求

| 依赖 | 版本/说明 |
| --- | --- |
| JDK | 17+（21 亦可） |
| Maven | 3.9.x |
| Android Studio / Gradle | 打开 `android/` 由 AS 生成 Gradle 8.7 wrapper；或命令行 `gradle wrapper` |
| Redis | 服务端运行必需（默认 `127.0.0.1:6379`，见 `server/src/main/resources/application.properties`） |
| 高德账号 | 申请“地图 + 定位”Android Key（见 `docs/SETUP_AMAP.md`） |
| Node（可选） | ≥22，用于 `server/smoke-test.mjs` 协议冒烟测试 |

---

## 🚀 快速开始

### 1. 服务端

```bash
cd server
mvn -DskipTests package                 # 产出 target/family-share-server.jar
java -jar target/family-share-server.jar # 需本机 Redis，监听 0.0.0.0:3000
curl http://127.0.0.1:3000/api/health   # -> {"status":"ok",...}
```

- **必须先配置** `server/src/main/resources/application.properties`：
  - `app.api-token`：与客户端 `API_TOKEN` 一致（访问口令）。
  - `app.bug-admin-token`：Bug 管理页 `/bugadmin/<token>` 的路径口令。
  - `spring.data.redis.password`：你的 Redis 密码（无密码留空）。
- 改端口：`java -jar target/family-share-server.jar --server.port=3001`。

### 2. 客户端

> **先决条件：放入高德轻量版地图SDK（本仓库不分发该 jar）**
> 本工程使用高德**轻量版地图SDK V1.3.2**，它**不在 Maven 仓库**，且因授权限制**不随本仓库分发**（`android/app/libs/*.jar` 已在 `.gitignore` 中忽略）。
> 请下载 <https://a.amap.com/lbs/static/amap_3dmap_lite/Lite3DMap.zip>，解压后把其中的 jar（形如 `Lite3DMap_1.3.2_AMapSearch_9.7.4_AMapLocation_6.4.9_*.jar`）放入 `android/app/libs/`（目录不存在就新建）。
> 详见 `docs/SETUP_AMAP.md` 第 6 节。

1. 按 `docs/SETUP_AMAP.md` 申请高德 Android Key（绑定包名 `com.family.share` + 你的签名 SHA1，勾选**地图 + 定位**）。
2. 编辑 `android/gradle.properties`：

   ```properties
   AMAP_KEY=<你的高德Key>
   SERVER_URL=https://<你的服务器域名或IP>[:端口]
   API_TOKEN=<与服务器 app.api-token 一致>
   ```

3. Android Studio → Sync → Run；或命令行：

   ```bash
   cd android
   ./gradlew assembleDebug        # app/build/outputs/apk/debug/app-debug.apk
   ./gradlew assembleRelease      # 发布包
   ```

### 3. 首次使用（两台手机）

1. 手机 A：进入 → 家庭设置默认「加入家庭」，请切到「**创建家庭**」→ 设昵称 → 得到 6 位家庭码（同意隐私说明）。
2. 手机 B：进入 → 「加入家庭」→ 输家庭码 → 提交申请，等 A 在 ⋮「消息」中同意后自动入群。
3. 两台都授权定位「始终允许」；⋮ → 权限设置 → 开启忽略电池优化、自启动管理（引导搜索）。
4. 地图显示彼此位置；点成员行看详情；⋮ → 一键刷新 / 消息 / 下线 / 检查更新。

---

## 📁 目录结构

```
FAMILYSHARE/
├── android/            # Android 客户端
│   ├── gradle.properties      # ★ AMAP_KEY / SERVER_URL / API_TOKEN
│   └── app/src/main/          # 源码、布局、资源
├── server/             # Spring Boot 服务端
│   ├── src/main/java/...      # ApiController / WsHandler / Store / 访问口令过滤
│   ├── src/main/resources/    # application.properties、官网/隐私/权利页、bugadmin
│   ├── pom.xml / run-server.bat / smoke-test.mjs / update.json
├── docs/               # PROTOCOL.md、SETUP_AMAP.md、app-icon.png
└── .gitignore
```

---

## 🔌 协议

- 完整 REST + WebSocket 协议见 [`docs/PROTOCOL.md`](docs/PROTOCOL.md)。
- 坐标统一为高德坐标系 GCJ-02；服务端只透传不转换。
- 身份模型：`deviceId`（首启生成的 UUID）即身份，`familyId` + 6 位 `code` 即家庭；加入家庭需群主同意。
- 访问口令：除 `/api/health`、`/downloads/**`、`/bugadmin/**` 白名单外，`/api/**`、`/ws`、`/icons/**` 都要带 `X-Api-Token` 或 `token`。

---

## 🗺️ 高德 Key
按 [`docs/SETUP_AMAP.md`](docs/SETUP_AMAP.md) 申请并配置（Android 平台 Key，绑定包名 + SHA1，勾选地图/定位服务）。

---

## ⚠️ 安全与免责

- 该设计**无账号体系**，家庭码即授权凭证、黑名单凭创建者身份判定；请妥善保管访问口令与家庭码。
- 位置数据保存在**你自己部署的服务器**；公网部署建议增加 HTTPS/WSS 反向代理，并把 `network_security_config.xml` 收紧为仅允许你的域名。

---

## 📮 提供服务器

如果你愿意为本项目提供可用的服务器，欢迎联系：**3557665817@qq.com**。

---

## 📄 License

本项目采用 [CC BY-SA 4.0](LICENSE)（知识共享 署名-相同方式共享 4.0 国际）许可协议开源，版权人：**KeepHope2901**。

- 你可以自由地**共享**（复制、发行）和**演绎**（修改、二次开发）本作品，包括商业用途。
- 但必须**署名**（注明作者并附许可协议链接、标明是否做了修改），且**衍生作品必须以相同协议（CC BY-SA 4.0）发布**。
- 完整许可协议：[LICENSE](LICENSE)；官方文本：<https://creativecommons.org/licenses/by-sa/4.0/legalcode.zh-hans>

> ⚠️ 若你在应用市场发布，请注意高德 SDK 的商用许可条款可能与开源许可存在差异，发布前请自行确认。

---

## 📋 更新日志

- 2026.09.12 修复多家庭下的两个问题：①**点「家庭码」误报"尚未加入家庭"**——它读的是旧的单家庭字段 `family_code`，切换家庭后该字段为空；现改为读取「当前家庭自己的家庭码」（`family_codes` 映射），并在新增家庭时同步写一份旧字段作为兜底。②**刷新期间切家庭，旧家庭的家人标签会临时跑到当前家庭**——根因是定位结果回调里读的是「当前家庭」而非「发起上报时的家庭」，且切换家庭时那次刷新仍会继续：现上报/广播全部绑定**发起时的家庭 ID**，并在服务里新增**家庭代号**（切换家庭时自增），代号不一致的定位结果直接作废；收到 WS 家人位置时也带上当前家庭 ID，前台按家庭过滤。切换家庭后会主动上报一次自己的位置。

- 2026.09.12 修复「切换家庭弹窗里没有家庭可选」：原实现用 `AlertDialog.setItems()` 渲染列表，且只依赖本地记录（本地记录与服务器不同步时列表就是空的）。现改为**打开弹窗时先请求 `GET /api/family/my` 拉取真实家庭列表**，再用手工构建的列表视图展示（每项显示家庭码、成员人数、群主标识、当前家庭打 ✓），列表底部附带「创建 / 加入家庭」入口；服务器不可用时回退到本地记录，若确实一个家庭都没有则给出创建/加入引导。
- 2026.09.12 按反馈调整多家庭交互与显示：①**去掉「左右滑动切换家庭」与左右两侧渐隐**，改为在「定位我」下方新增**「切换家庭」按钮**（弹出已加入家庭列表供选择，当前家庭标 ✓；只有一个家庭时提示可创建/加入，原有家庭会保留）；②标题栏**去掉「第 x/y 个家庭」的数字编号**，只显示「家庭码 · 家庭成员」；③修复**家庭数据串台**：切换家庭后，上一个家庭迟到的成员列表响应/位置广播会盖到新家庭上（表现为"第一个家庭刷新好的人出现在第二个家庭"）——新增**家庭代号**机制，切换/解散/被移出时代号自增，所有成员列表响应与位置广播都先校验家庭代号与家庭 ID，不匹配一律丢弃；位置广播也带上家庭 ID 以便客户端核对；④清理掉为轻量版 WebView 地图写的缩放期文字抑制死代码，并**去掉对 MapView 的 `setLayoutParams`**（原生 3D SDK 下会触发 GL 表面重建，可能引发地图原生线程崩溃）。

- 2026.09.12 修复「成员面板被撑满整屏、地图看不见」：新加的「左右滑动切换家庭」渐隐层用了 `layout_height="match_parent"`，而它的父容器（成员列表外层 FrameLayout）是 `wrap_content`——被这两个全高子视图一撑就变成整屏高，进而把 `panelBody`/面板高度算成整屏、把地图完全挤掉（用 `adb shell uiautomator dump` 量到 `bottomPanel` 高 2578px 定位到）。现改为**单个固定高度（72dp）的左右双向渐变层**（`bg_fade_horizontal`，一层同时画左、右两侧淡出），删掉了两个单侧渐隐文件；并复核整个布局，除根容器与地图外已无 `match_parent` 高度的视图。

- 2026.09.12 **修复「打开即闪退」**：换回完整版 3D 地图 SDK 时，把地图初始化（含 `initScaleBar()` / `updateScaleBarPosition()`）提前到了面板相关 View 还没 `findViewById` 之前，`onCreate` 里就访问了还是 null 的 `bottomPanel`，抛 `NullPointerException: View.getWidth() on a null object reference` 导致一启动就崩（已用 adb 抓到堆栈定位）。现改为：地图对象同步就绪（`getMap()`）后**只注册相机监听**，面板/刻度尺/标点的初始化统一推迟到所有 View 就绪后（`postPanelReadyInit()` → `mapView.post`），并给 `updateScaleBarPosition()` / `panelFullHeight()` / `panelHeaderHeight()` 加空值兜底；设备实测启动正常、无异常日志。

- 2026.09.12 新增**多家庭支持**：同一设备可同时属于多个家庭，**创建/加入新家庭不再退出原有家庭**（服务端创建家庭与审批加入都不再把设备从其它家庭移除；新增 `GET /api/family/my` 列出我加入的全部家庭）。主页面成员列表**左右滑动即可切换家庭**（左滑看下一个、右滑看上一个，竖直方向仍是滚动列表），标题栏显示「家庭码 · 第 x/y 个家庭」；存在多个家庭时列表左右边缘有**透明渐隐 + 呼吸式「左右滑动可切换家庭」提示**（与上/下的成员渐隐提示共存）。群主菜单新增**「解散家庭」**（二次确认后删除家庭，全员收到 WS `family-disbanded` 并移除该家庭；本机若还有其它家庭会自动切换过去，否则回到创建/加入引导）。被移出某个家庭时也只移除该家庭，不再清空全部。家庭列表与各家庭的家庭码存于本地（`family_ids` / `family_codes`），旧版单家庭数据自动迁移。

- 2026.09.12 **地图 SDK 从「轻量版 V1.3.2（WebView 渲染）」换回「完整版 3D 地图 SDK 9.8.3（原生渲染）」**：轻量版是 WebView + 高德 JS API，底图（瓦片）与地名标注分属两层、刷新节奏不同，缩放动画期间地名会"比底图跑得快"（高德官方 FAQ 也说明地图移动过程中底图不实时刷新），应用层无法修正；完整版为原生渲染、底图与标注同层，从根上消除该现象。改动：`app/build.gradle` 改回 Maven 依赖 `com.amap.api:3dmap:9.8.3`（**已内置定位，不要再单独加 `com.amap.api:location`，否则类重复构建失败**）、删除 `app/libs/` 下手动放置的轻量版 jar；`MainActivity` 由异步 `getMapAsyn()` 改回同步 `getMap()`。release APK 由约 3.3MB 增至 **17MB**（这是为渲染正确性做的取舍）。同时「缩放期文字抑制」默认关闭（原生渲染下已无必要，常量仍保留以便将来切回轻量版）。
- 2026.09.12 按用户要求删除了上一版加入的两个排查用入口（⋮ 菜单的「隐藏底图文字」与「标点微调」对话框），相关字段/字符串一并清理。针对「缩放动画中底图地名比底图跑得快」新增**缩放期文字抑制**（`HIDE_MAP_TEXT_WHILE_ZOOMING`，默认开启）：缩放过程中不绘制底图文字、动画结束再画，地名不再拖影漂移（代价是缩放瞬间地名短暂消失后出现在正确位置）；同时保留**地图容器尺寸对齐**（地图就绪/布局变化/从后台返回时重新测量 WebView 容器），排除容器尺寸过期导致标注层按错误比例缩放。若仍无改善，可改为关闭底图文字，或换回完整版 3D 地图 SDK（原生渲染、底图与标注同层）。
- 2026.09.12 排查「缩小地图时底图地名（如"武汉"）比底图跑得快、缩放结束后又对齐」：高德官方 FAQ 明确说明 JS API / 轻量版在**地图移动（含缩放动画）过程中底图不会实时刷新**，底图与标注是两层、刷新节奏不同，属 SDK 行为，应用层无法直接修正。为排除"容器尺寸过期导致标注层按错误比例缩放"，本版新增**地图容器尺寸对齐**：地图就绪、布局变化、从后台返回时都会把 WebView 容器重新测量并按新尺寸重算视野比例（若原本是尺寸不一致引起的，这一项即可缓解）。另在 ⋮ 菜单提供「隐藏底图文字」，若不需要底图地名，可一键只保留家人标点（可改成默认）。
- 2026.09.12 面板拖拽体验修正：撤销"拖到阈值即时判定"（它会让手指还没松开面板就弹回去），改为**只在松手时判定**，并按"拉出的绝对距离"而不是"占最大位移的比例"决定去向——收起态上拉超过 4dp 即展开、展开态下拉超过 16dp 才收起，快速上/下滑仍按方向直接切换；拖动过程中允许随时反向跟随手指。成员列表去掉「还有 N 位家人」文字提示（仅保留上/下透明渐隐，被裁掉的那半行自然淡出）。
- 2026.09.12 面板/列表交互按反馈重做：①展开面板不必再"拉够距离"——从收起态上拉到 22dp 就即时展开（拖到阈值立即提交，不用等松手），快速上滑同样直接展开；②灰色拖拽条的触摸区从 88×32dp 扩大到「整行宽 × 40dp」，中间细条视觉不变，更容易按住；③**移除了「列表到顶后继续下拉收缩面板」功能**（列表只负责滚动，收起请用灰色横条/标题行下拉）；④列表高度改为「3 行 + 下一行露出约 40%」，能看清下一个家人是谁。修正「还有 N 位家人」少算一个的问题：只露出一部分的那一行现在也计入（5 个成员不再显示成"还有 1 位"）；列表在上方被裁剪时也会显示「上方还有 N 位家人，下滑查看」。另新增「标点微调」（⋮ 菜单）：可按 2dp 步进上下微调家人标点的垂直位置（个别机型密度/DPR 不一致导致的几像素偏差可现场校准），并提供「改用官方内置标点」逃生开关用于排查是否出自自绘位图的像素换算。
- 2026.09.12 修复了标点在缩放时相对坐标点的位置关系变化：低缩放下的圆点样式原来把锚点放在了名字标签的下边缘（导致整枚标点相对真实位置下移约一个标签高度），现在改为「名字标签在上、圆点在下」，锚点正好落在圆点底部，与放大时的卡片尾尖压点方式一致，切换样式时位置不再跳动。列表被裁剪时新增半截行 + 透明渐变 + 「还有 N 位家人，上滑查看」提示（上下都有渐隐），一眼能看出下面还有成员。面板手势重写：从收起态上拉只需拉出 30%（快速上滑直接展开）即可展开，不再需要拉很长；松手判定加入甩动速度；修复「下拉列表只能收缩一次、之后失效」以及中途手势被打断导致状态错乱的问题，并保证「上滑到顶的那一次不会误触发收缩，需到顶后再下拉」。
- 2026.09.12 新增「隐藏底图文字」诊断开关（⋮ 菜单）：轻量版地图SDK 的城市/地名文字由 SDK 内置 JS 绘制、无法逐层控制，若个别机型在缩放时出现底图文字错位，可用该开关判断问题出在底图文字层还是瓦片/GPU 渲染层。
- 2026.09.12 修复了部分手机上「缩小地图后标点与名字文字相对真实位置偏移」的问题：轻量版地图SDK 用「位图像素 ÷ 屏幕密度」在网页里设置图标尺寸与偏移，位图宽高不能被密度整除时两侧各自取整就会漂移——现在标点位图宽高按屏幕密度取整、锚点严格取位图底边中点（尾巴尖端正好压点），任意缩放下位置一致。同时优化了界面：地图缩小（zoom<13）时标点自动折叠为小圆点+名字小标签，放大后展开为完整卡片（互相压字的问题随之消失）；精度圈在缩放较小或家人过多时自动隐藏；轨迹线按时间渐变（旧点淡、新点亮的）；成员面板改为卡片式列表（行卡片、头像描边、空状态引导卡片带「创建/加入家庭」按钮）；面板展开带轻微回弹与渐显。----- 提交于 2026.09.12
- 2026.09.12 修复隐藏问题：①定位请求被新请求覆盖时，上一次的等待方永远收不到回调（连续点「刷新」会卡住）——现在会先取消上一次再开始；②家里只有 1 个定位点时用边界适配视野可能跳到异常缩放——改为固定级别定位；③换头像后地址不变导致标点/列表头像仍显示旧图——上传成功后强制重取；④服务器播放铃声失败时仍弹出「正在响铃」通知和停止按钮——失败不再提示；⑤标点图标原来每次位置刷新都重建（SDK 还会把每张图标永久留在内存里）——改为按昵称/头像缓存，长时间运行更省内存、更省电；⑥关闭共享后系统级看门狗任务未取消，会每 15 分钟白唤醒一次——已一并取消。----- 提交于 2026.09.12
- 2026.09.12 服务端安全与健壮性修复：成员列表/黑名单校验请求者确属该家庭、入群申请状态只允许申请人本人查询、群主不能把自己移出/拉黑自己；修复「删除对账永不生效」导致的 Redis 陈旧键堆积（原实现会遍历本轮即将重新写入的同一个索引集合）；Redis 载入改为后台线程，服务启动不再被 Redis 阻塞；入群申请、群主转让、成员移出等关键变更立即落库（不再依赖 2 秒周期）；WebSocket 连接建立的成员状态更新与家庭广播加锁/取快照，避免并发修改；申请/家庭 ID 改用 UUID；Bug 管理页口令改为配置项 app.bug-admin-token。----- 提交于 2026.09.12
- 2026.09.12 版本号对齐：`server/update.json` 的 versionCode/versionName 改为与客户端一致（1 / 1.0.0）。客户端检查更新实际按 APK 的 MD5 比对，此项仅消除配置层面的不一致。----- 提交于 2026.09.12
- 2026.09.10 因授权限制，高德轻量版地图SDK 的 jar 不再随仓库分发（`android/app/libs/*.jar`、`*.aar` 已加入 `.gitignore`）；客户端构建说明新增「先决条件：自行下载 Lite3DMap.zip 并把 jar 放入 `android/app/libs/`」。----- 提交于 2026.09.10
- 2026.09.10 地图改用高德「轻量版地图SDK」V1.3.2（WebView 渲染、无原生 .so，合包含定位 6.4.9）：移除 Maven 的 `3dmap` 依赖，改为把 SDK jar 放入 `app/libs/`；地图对象改为异步就绪（`getMapAsyn`，就绪前跳过地图操作、就绪后补画标点/轨迹并定位相机）；移除轻量版不支持的指南针/缩放按钮/默认刻度尺设置与地图隐私接口调用。release APK 由约 17 MB 进一步降到约 3.3 MB。----- 提交于 2026.09.10
- 2026.09.10 减小安装包体积：release 构建开启混淆（R8）与资源压缩（minifyEnabled + shrinkResources），并只打包 ARM 真机 ABI（去掉 armeabi / x86_64 的原生库）。release APK 由约 28 MB 以上降到约 17 MB；同时补齐高德地图与 ZXing 的混淆保留规则，避免混淆后运行异常。注：如需在 x86 模拟器安装 release 包，请自行放开 ABI 过滤。----- 提交于 2026.09.10
- 2026.09.10 改进后台保活：新增系统级 JobScheduler 看门狗（15 分钟周期、重启后保留，与闹钟/前台服务多路兜底）；未加入电池优化/自启动白名单时每 3 天温和提醒一次。「关于」新增 GitHub 开源仓库地址；官网下载按钮由华为应用市场改为跳转 GitHub，隐私政策与个人信息保护权利页同步加入 GitHub 仓库链接。服务端新增只读状态看板（/dashboard/<token>：总用户数、在线设备、家庭数、CPU、内存、入站/出站带宽与压力波动曲线），仅聚合统计、不含任何隐私信息，也不提供任何修改入口。----- 提交于 2026.09.10
- 2026.08.30 详情页保留刻度尺（不再消失）；「关于」新增备案号（鄂ICP备15020522号-2A）；加入家庭页的「扫码加入」改为醒目按钮，且扫码识别后直接确认加入（无需再点「确定」）。----- 提交于 2026.08.30
- 2026.08.30 新增二维码：家庭码弹窗数字下方显示二维码；加入家庭弹窗在「加入家庭」选项的两个输入框下方新增「扫码加入」（灰色小字），相机权限仅在点击扫码时才申请；成员列表：群主置顶；成员超过 4 个时第 5 个露出一半作为可滚动提示。----- 提交于 2026.08.30
- 2026.08.30 优化了列表下拉收缩的触发：只有列表“本就已在顶部、再往下拉”时才收缩；往下拉到顶的这一下不算，需到顶后重新下拉才收缩（避免滚到顶就误收起），上滑不再可能误触发收缩。----- 提交于 2026.08.30
- 2026.08.30 调整了刷新与面板交互：家人位置更新时顶部提示「xxx 的位置已更新」（限频防刷屏）；展开态上滑仅滚动成员列表、不再误收起面板；灰色横条那一行任意位置均可上滑展开面板；成员列表最多同时显示 4 行、多余可上下滑动，滚到顶部后继续下拉收起面板。----- 提交于 2026.08.30
- 2026.08.29 更新了入群申请：改为“上线自动检查、同意即自动加入”，关掉等待弹窗/重开 App 后仍会继续检查；等待弹窗文案同步。修复了：只有当场同意才能加入、关闭弹窗后无法加入需重新申请的问题。----- 提交于 2026.08.29
- 2026.08.29 更新了：请求刷新位置改为顶部文字提示；所有子菜单「取消」按钮改为「返回」；面板手势方向修正（收起上滑展开、展开下滑收缩、展开上滑不收缩）；成员列表最多同时显示 4 行、更多可上下滑。----- 提交于 2026.08.29
- 2026.08.29 更新了官网首页（轨迹改为绿色起点展示、新增黑名单/消息中心/应用内更新/服务器切换等说明）；GitHub README 加入官网链接，项目名统一为「家庭共享」。----- 提交于 2026.08.29
- 2026.08.29 更新了轨迹（去掉方向箭头、新增单独的绿色起点标记）、官网首页（加入 GitHub 开源仓库链接与“可自建服务器”说明）、响铃时长自定义（10/30/60/90/120 秒或自定义 1-300 秒）、打开应用自动刷新响铃状态（便于显示“关闭响铃”按钮）。----- 提交于 2026.08.29
- 2026.08.29 更新了服务器切换（⋮ 菜单“切换服务器”，可选官方/自定义、可添加/删除）、网络层对瞬时网络/DNS 错误自动重试并给出友好提示；修复了切换服务器后绿点/灰点消失、个别用户 DNS 解析失败。----- 提交于 2026.08.29
- 2026.08.29 家庭设置默认“加入家庭”与家庭码输入框初始可见、打开应用自动向全员请求一次实时位置、自启动管理改为引导搜索；修复了加入家庭后看不到其他成员、家庭码输入框需先切走再切回才出现。----- 提交于 2026.08.29

**Star ⭐ 如果这个项目对你有帮助！**
